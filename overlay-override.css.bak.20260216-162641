:root{
  --overlay-alpha: 0.08;
  --overlay-rgb: 255,255,255;
}

/* Main site tint layers */
.sw-overlay,
.sw-overlay::before,
.sw-overlay::after,
#section-hero::before,
#section-cta::before {
  background: rgba(var(--overlay-rgb), var(--overlay-alpha)) !important;
  opacity: 1 !important;
  backdrop-filter: none !important;
  -webkit-backdrop-filter: none !important;
}

/* Keep readability while revealing background */
#section-hero .text-center,
#section-cta .text-center {
  text-shadow: 0 2px 10px rgba(0, 0, 0, 0.32);
}
